#include<stdio.h>
void main()
{
    int i[6],a[i]=n;
    n=2001,2000,2002,2003,2004,2005;
    for(a[i]=0;a[i]<6;a[i]++)

    if((a[i]%400==0)&&(a[i]%100!=0)||(a[i]%4==0))
      printf("%d is leap year number\n",a[i]);
        else
        printf("%d is not leap year number\n",a[i]);
}
